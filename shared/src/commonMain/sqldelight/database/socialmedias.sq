CREATE TABLE socialMediaEntity(
    id INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
    name TEXT NOT NULL,
    daysCount INTEGER NOT NULL
);

getSocialMedias:
SELECT *
FROM socialMediaEntity;

insertSocialMediaEntity:
INSERT OR REPLACE
INTO socialMediaEntity(
    id,
    name,
    daysCount
)
VALUES (?, ?, ?);